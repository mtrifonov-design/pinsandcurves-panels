

- **Logical Layer**\
Describes a GPU workload as a pure function of inputs, producing an immutable graph of resources and operations.

- **Physical Layer**\
Consumes such a graph and executes it efficiently, optimizing for repeated invocations with minimal state changes.