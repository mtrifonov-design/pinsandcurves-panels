

function Layout() {
    return <div style={{backgroundColor: "var(--gray1)", position: "absolute", top: 0, left: 0, right: 0, bottom: 0, pointerEvents: "none",}}>
        
    </div>;
}

export default Layout;